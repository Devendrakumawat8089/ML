[project]
name = "deltro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Devendra Kumawat", email = "devendrakumawat8089@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
deltro = "deltro:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
